---
import type { HTMLAttributes, HTMLTag } from 'astro/types'

interface Props extends Omit<HTMLAttributes<'section'>, 'slot'> {
  as?: Extract<HTMLTag, 'section' | 'article' | 'footer' | 'nav' | 'div'>
  center?: boolean
}

const { as: Tag = 'section', center = true, class: extraClass } = Astro.props
---

<Tag class:list={[{ 'mx-auto w-full max-w-7xl px-8': center }, extraClass]}>
  <slot />
</Tag>
